const React = require('react');

class Show extends React.Component {
  render() {
    return (
      <div>
        <h1>This is the Show Page, Single Items</h1>
      </div>
    );
  }
}

module.exports = Show;
